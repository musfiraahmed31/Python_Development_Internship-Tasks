#=== gradio installation
pip install gradio==4.25.0

#== pip installation
pip install -r requirements.txt

# === Core Application Dependency ===
gradio==4.25.0

# === Development & Linting (Optional but Useful) ===
black>=24.4.0         # Code formatter
flake8>=7.0.0         # Python linter

# === Optional Deployment (Future Proof) ===
fastapi>=0.110.0      # For optional backend API
uvicorn[standard]>=0.29.0  # ASGI server for FastAPI

# === Environment Variable Management ===
python-dotenv>=1.0.1  # Load environment variables from .env

# === Hugging Face Deployment (Optional) ===
huggingface_hub>=0.21.4  # For pushing to Hugging Face Spaces

# === Terminal Enhancements (Optional Dev UX) ===
colorama>=0.4.6       # Colored terminal outputs (for local dev)

# === Version Specified for Python Interpreter Info (Documentation Only) ===
# Requires: Python >=3.8
